# Baked into deployed builds (Vercel has no env config for this project).
# Local preview overrides these via .env.production.local (gitignored) so the
# vite proxy keeps handling /api.
VITE_API_URL=https://api.pyvotmynt.in
VITE_DASHBOARD_URL=https://app.pyvotmynt.in
